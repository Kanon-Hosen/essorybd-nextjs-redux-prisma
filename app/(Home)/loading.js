const loading = () => {
    return (
        <div className='flex w-full h-screen items-center justify-center text-lg font-semibold'>
            <h1>Loading....</h1>
        </div>
    );
};

export default loading;